# Design

The hello implementation is a single function `hello()` (`app/src/hello.ts`). It is a pure function. It takes no arguments and only returns the fixed string `"Hello Claude!"`. It has no state and no external dependencies.
