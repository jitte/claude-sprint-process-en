# Overview

hello is the smallest sample project imported with this harness. It is a TypeScript project with a single component. When it runs, it only returns the fixed string `Hello Claude!`. It exists as the smallest sample on which the README import procedure, the sprint process, and the gate tools all run from start to end.
